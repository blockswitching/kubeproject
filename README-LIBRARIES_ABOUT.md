| Package              | Purpose                                                                                              |
| -------------------- | ---------------------------------------------------------------------------------------------------- |
| `aiohappyeyeballs`   | Improves async network connection speed and reliability for IPv4/IPv6. Used internally by `aiohttp`. |
| `aiohttp`            | Async HTTP client/server library for Python. Useful for high-speed API calls.                        |
| `aiosignal`          | Signal/event handling support for async applications. Used by `aiohttp`.                             |
| `asgiref`            | ASGI utilities required by Django for async support.                                                 |
| `attrs`              | Helps create clean Python classes with less boilerplate code.                                        |
| `certifi`            | Provides updated SSL certificates for secure HTTPS requests.                                         |
| `charset-normalizer` | Detects text encoding in HTTP responses. Used by `requests`.                                         |
| `Django`             | Python web framework used to build web applications and APIs.                                        |
| `durationpy`         | Parses and handles time durations like `5m`, `2h`, etc. Often used in Kubernetes tools.              |
| `frozenlist`         | Immutable list structure used internally by async libraries.                                         |
| `idna`               | Supports international domain names in URLs.                                                         |
| `kubernetes`         | Official Python client for Kubernetes API. Lets Python manage clusters, pods, deployments, etc.      |
| `multidict`          | Dictionary structure supporting multiple values per key. Used by `aiohttp`.                          |
| `oauthlib`           | OAuth authentication implementation.                                                                 |
| `propcache`          | Property caching utility for better performance.                                                     |
| `python-dateutil`    | Advanced date and time parsing utilities.                                                            |
| `PyYAML`             | Reads and writes YAML files (`deployment.yaml`, Kubernetes configs).                                 |
| `requests`           | Popular Python HTTP library for API calls.                                                           |
| `requests-oauthlib`  | OAuth support for the `requests` library.                                                            |
| `six`                | Compatibility layer for Python 2 and 3 code.                                                         |
| `sqlparse`           | SQL parser used internally by Django.                                                                |
| `typing_extensions`  | Extra typing features for Python type hints.                                                         |
| `tzdata`             | Timezone database support.                                                                           |
| `urllib3`            | Low-level HTTP client used by `requests`.                                                            |
| `websocket-client`   | WebSocket communication library. Useful for real-time Kubernetes logs/events.                        |
| `yarl`               | URL handling library used by async frameworks.                                                       |
